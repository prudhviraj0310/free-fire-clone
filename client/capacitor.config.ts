import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.ttcecs.app',
    appName: 'Free Fire Tournament',
    webDir: 'out',
    server: {
        allowNavigation: ['ucs48k4c8g80840k8c00w4cc.hexaind.org']
    }
};

export default config;
